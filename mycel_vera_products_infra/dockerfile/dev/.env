# mycel_vera_products/docker/dev/.env
# Runtime + build configuration for dev environment.
# Secrets → .env.secrets (gitignored).

# ── Build vars ────────────────────────────────────────────────────────────
ROOT_MODULE_NAME=mycel_vera_products
SUBMODULES=
CORE_VERSION=v0.1.0

# ── Port configuration ────────────────────────────────────────────────────
SERVICE_PORT=9100
WEB_PORT=9102
MCP_PORT=9101
MOBILE_PORT=9103

# ── Runtime environment ───────────────────────────────────────────────────
LOG_LEVEL=DEBUG
PYTHONOPTIMIZE=0

# ── Service metadata ──────────────────────────────────────────────────────
SERVICE_NAME=mycel_vera_products_service
SERVICE_VERSION=0.1.0

# ── Observability (OTel) ─────────────────────────────────────────────────
# Service (Python, gRPC inside Docker network)
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
# Web (browser, HTTP on host)
VITE_OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
